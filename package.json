{
  "name": "probability-queue",
  "version": "0.1.0",
  "description": "Structure for drawing items with specified probabilities",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "test_coverage": "istanbul cover _mocha --include-all-sources --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kcwiakala/probability-queue.git"
  },
  "keywords": [
    "random",
    "pick",
    "probability",
    "draw",
    "probability",
    "queue"
  ],
  "author": "kcwiakala",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.1.2",
    "coveralls": "^3.0.0",
    "istanbul": "^0.4.5",
    "mocha": "^4.0.1",
    "mocha-lcov-reporter": "^1.3.0"
  }
}
